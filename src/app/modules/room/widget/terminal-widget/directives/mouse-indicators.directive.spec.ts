import { MouseIndicatorsDirective } from './mouse-indicators.directive';

describe('MouseIndicatorsDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseIndicatorsDirective();
    expect(directive).toBeTruthy();
  });
});
